StarHub Demo (PC Web) — minimal frontend

快速开始:
1. 在项目目录运行: npm install
2. 启动后端（参考 demo 方案，FastAPI 在 http://localhost:8000）
3. 启动前端: npm run dev

注意:
- 本端仅为 Demo 前端骨架，需后端接口按文档提供 /artists /ai/generate /shows 等接口
- 若后端不在本机，请修改 src/api.js 中 baseURL
